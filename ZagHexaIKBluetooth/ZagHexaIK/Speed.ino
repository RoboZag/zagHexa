void speedSet()
{
  // Set speed
  if (speedLevel == 1) {
    // Set velocity to certain value
    velocity = 30;
  }
  else if (speedLevel == 2) {
    // Set velocity to certain value
    velocity = 45;
  }
  else if (speedLevel == 3) {
    // Set velocity to certain value
    velocity = 65;
  }

  else if (speedLevel == 4) {
    // Set velocity to certain value
    velocity = 85;
  }
}
